<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, minimum-scale=1, initial-scale=1, user-scalable=yes">
    {{#extraHeader}}
      {{{.}}}
    {{/extraHeader}}
    <title>{{foaf:name}}</title>
  </head>
  <body>
  <style>
  </style>
          <h2>{{foaf:name}}</h2>

          <div class="row">URI: <a href="{{@id}}" target="_blank">{{@id}}</a></div>
          <div class="row">Actor Type: {{#ofd:actorType}} {{.}}, {{/ofd:actorType}} </div>
          <div class="row">Web: {{#schema:url}}
          <div> <a href="{{.}}" target="_blank">{{.}}</a></div>
          {{/schema:url}}
          </div>
          <div> <h2>Types of Food:</h2>
          {{#ofd:offersFoodType}}
              <div>{{.}}</div>
          {{/ofd:offersFoodType}}
          </div>
  </body>
</html>